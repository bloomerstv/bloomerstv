query StreamChats($profileId: String!) {
  streamChats(profileId: $profileId) {
    content
    handle
    avatarUrl
    profileId
    id
    createdAt
    txHash
    formattedAmount
    currencySymbol
    authorProfileId
  }
}
