mutation CreateMyLensStreamSession(
  $postId: String!
  $viewType: ViewType
  $sessionId: String
) {
  createMyLensStreamSession(
    postId: $postId
    viewType: $viewType
    sessionId: $sessionId
  )
}
