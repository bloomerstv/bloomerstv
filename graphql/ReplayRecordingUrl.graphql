query StreamReplayRecording(
  $postId: String
  $accountAddress: String
  $sessionId: String
) {
  streamReplayRecording(
    postId: $postId
    accountAddress: $accountAddress
    sessionId: $sessionId
  ) {
    postId
    recordingUrl
    premium
    createdAt
    sessionId
    accountAddress
  }
}
