query MyStream {
  myStream {
    accountAddress
    streamName
    streamDescription
    lastSeen
    playbackId
    streamKey
    isActive
    latestSessionId
    latestSessionCreatedAt
    premium
    isHealthy
    issues
    nextStreamTime
  }
}
